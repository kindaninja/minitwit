apiVersion: 1

providers:
  - name: 'System'
    folder: 'All'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/system

  - name: 'API'
    folder: 'All'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/api